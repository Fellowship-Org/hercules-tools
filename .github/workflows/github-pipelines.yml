name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        fetch-depth: 5  # Fetch 5 commits worth of history

    - name: Set up Git and Curl
      run: |
        sudo apt-get update
        sudo apt-get install -y git curl
        git --version  # Verify Git installation
        curl --version # Verify curl installation

    - name: Cache dependencies (Git & Curl)
      uses: actions/cache@v2
      with:
        path: |
          /usr/bin/git
          /usr/bin/curl
        key: ${{ runner.os }}-git-curl-${{ hashFiles('**/*.sh') }}
        restore-keys: |
          ${{ runner.os }}-git-curl-

    - name: Analyze Changes
      run: |
        chmod +x ./analyze_changes.sh
        ./analyze_changes.sh HEAD~1 HEAD
        
    - name: Test Pipeline Failure Handling
      if: github.ref == 'refs/heads/test-failures'
      run: |
        # Simulate a build failure for testing pipeline error handling
        echo "Testing pipeline failure scenarios"
        exit 1
    - name: Syntax error test
      run: |
        echo "This will cause a syntax error
